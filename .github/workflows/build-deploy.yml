# Build the Pelican site and publish it to GitHub Pages.
#
# The build runs on every push and pull request; only pushes to main deploy.
# A build that fails, emits a Pelican warning, or trips scripts/check_build.py
# never reaches the deploy job.
name: Build and deploy

on:
  push:
    branches: ["main"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

# One deployment at a time. Do not cancel a run in progress — it may be
# midway through publishing.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Full history: the sitemap takes each page's lastmod from the date its
      # source was last committed, and a shallow clone has no history to ask.
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: pyproject.toml

      # The Nu Html Checker that html5validator wraps is a Java program.
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      # An Insight's scientific source is LaTeX; content/insights/*.rst is
      # generated from it. pandoc lets CI regenerate those and fail if the
      # committed file does not match, which is what stops anyone correcting
      # the science in the generated file. No LaTeX here: the PDF is built
      # locally for Zenodo, and CI does not need it.
      - name: Install pandoc
        run: sudo apt-get update && sudo apt-get install -y pandoc

      - name: Install
        run: pip install -e ".[dev,test]"

      - name: Test
        run: make test

      # `make check` builds with publishconf (absolute URLs, analytics,
      # consent banner) under --fatal warnings, then smoke-tests output/.
      - name: Build and check
        run: make check

      - name: Validate markup
        run: make validate

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: output-publish

  deploy:
    # Pull requests build and check, but never publish.
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
